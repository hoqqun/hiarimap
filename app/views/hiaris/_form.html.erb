<%= form_for(@hiari, url: hiaris_path, html: {multipart: true}) do |f| %>
  <% if @hiari.errors.any? %>
    <div id="error_explanation">
      <h2><%= @hiari.errors.count %>件のエラーがあります。</h2>

        <ul>
        <% @hiari.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
    </div>
  <% end %>

  <h4>目撃日時を入力してください</h4>
  <div class="field">
    <div class='input-group date datepicker'>
  <%= f.text_field :discover_ari_date, class: 'form-control' %>
  <span class="input-group-addon">
    <span><%= fa_icon :calendar %></span>
  </span>
</div>
  </div>
  <h4>タイトルを入力してください</h4>
  <div class="field">
    <%= f.text_field :title, class:'form-control', placeholder: "タイトル" %>
  </div>

  <h4>コメントや詳細情報を入力してください</h4>
  <div class="field">
    <%= f.text_area :comment, size: "60x12", class:'form-control', placeholder: "内容" %>
  </div>
  <h4>目撃した住所(駅名、公園名など可能)を指定し、住所検索ボタンを押してください</h4>
  <h4>その後、マーカーをドラッグして位置を微調整できます</h4>
  <div class="field">
    <%= f.text_field :address, class: "form-control",placeholder: "目撃住所" %>
  </div>
  <div class="actions">
    <input type="button" value="住所検索" id="button1" class="btn btn-default" >
  </div>
  <div class="col-xs-offset-2 col-xs-10 col-lg-offset-4 col-lg-6">
    <div id="map_new" style='width: 600px; height: 400px;'></div>
  </div>
  <div class="field">
    <%= f.text_field :latitude, class: "form-control",placeholder: "緯度", readonly: true %>
  </div>

  <div class="field">
    <%= f.text_field :longitude, class: "form-control",placeholder: "経度", readonly: true  %>
  </div>


  <h4>画像があればファイルを選択してください。</h4>
  <div class="field">
    <%= image_tag @hiari.image if @hiari.image? %>
    <%= f.file_field :image, class: "form-control" %>
    <%= f.hidden_field :image_cache %>
  </div>

  <hr>

  <div class="actions">
    <%= f.submit '投稿', class:'btn btn-default' %>
  </div>
<% end %>

<%= link_to "投稿一覧画面にもどる", hiaris_path %>